#pragma once

// Shared definitions for the Rvrse Monitor native components will live here.
// Keep the interface stable so both the UI and plugins can depend on it.
namespace rvrse
{
    constexpr const wchar_t *kProductName = L"Rvrse Monitor";
}
